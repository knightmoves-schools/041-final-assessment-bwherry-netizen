BEGIN TRANSACTION;

INSERT INTO Ingredient (id, name)
VALUES (10, 'Nutmeg');

INSERT INTO RecipeIngredient (id, recipe_id, ingredient_id, measure_id, amount)
SELECT
    14,
    recipe_id,
    10,
    measure_id,
    amount
FROM RecipeIngredient
WHERE ingredient_id = 9;

DELETE FROM RecipeIngredient
WHERE ingredient_id = 9;

COMMIT;
